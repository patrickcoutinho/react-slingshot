version: 2.1

defaults: &defaults
  docker:
    - image: node:current-alpine3.12
  working_directory: ~/project

jobs:
  build:
    <<: *defaults
    steps:
      - checkout
      - run:
          name: "NPM install"
          command: "npm i"
      - run:
          name: "Lint code"
          command: "npm run lint"

workflows:
  say-hello-workflow:
    jobs:
      - build
